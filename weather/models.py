from django.db import models

# Create your models here.


# class SearchWeather(models.Model):
#     search_city = models.CharField(max_length=50, unique=True, verbose_name="Искомый город")
#     date_search = models.DateTimeField(auto_created=True, verbose_name="Дата поиска")
#     user = models.ForeignKey()

